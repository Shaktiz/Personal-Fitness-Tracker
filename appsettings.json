{
  // appsettings.json — Application configuration file
  // Logging levels and app URLs are configured here.
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
